В чат можно перейти через "Дополнительно" - "Общение"

Пыталась реализовать отправку массива в LocalStorage, (в выводе использовала JSON.parse(localStorage.getItem(i))), 
но он не совсем корректно выводит (просто строку элементов). 

Если нужно красиво вывести только сообщение (без доп. данных), то у меня это работает, но я заккоментила,
чтобы продемонстрировать некорректную работу.

LocalStorage хранит данные только одного контейнера, поэтому сохранение общения только с 1 пользователем
